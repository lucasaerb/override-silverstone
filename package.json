{
  "name": "override-silverstone",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "start": "node server.mjs",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:e2e": "vite build && node scripts/e2e.mjs"
  },
  "dependencies": {
    "three": "^0.185.0",
    "trystero": "^0.25.2"
  },
  "devDependencies": {
    "@types/three": "^0.185.0",
    "puppeteer": "^24.0.0",
    "typescript": "^5.6.0",
    "vite": "^6.0.0",
    "vitest": "^3.0.0"
  }
}
